from setuptools import setup

# The setup.py file is only present to support editable installs. In the
# future we will probably replace setup.py and setup.cfg by pyproject.toml
#
# https://setuptools.pypa.io/en/latest/userguide/pyproject_config.html#configuring-setuptools-using-pyproject-toml-files
setup()
